<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SportShoes - Sua Loja de Tênis</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="container">
                <a href="/<%= typeof isAdmin !== 'undefined' && isAdmin ? '?admin=true' : '' %>" class="logo">
                    <i class="fas fa-running"></i> Sport<span>Shoes</span>
                </a>
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/?esporte=Corrida">Corrida</a></li>
                    <li><a href="/?esporte=Basquete">Basquete</a></li>
                    <li><a href="/?esporte=Casual">Casual</a></li>

                    <% if (!isLoggedIn) { %>
                        <li><a href="/auth/login" class="login-btn">Entrar</a></li>
                        <li><a href="/auth/registro" class="register-btn">Cadastrar</a></li>
                    <% } else { %>
                        <% if (isAdmin) { %>
                            <li><a href="/admin" class="admin-btn">Admin</a></li>
                        <% } %>
                        <li><a href="/auth/logout" class="logout-btn">Sair</a></li>
                    <% } %>
                </ul>

                <div class="menu-toggle">
                    <i class="fas fa-bars"></i>
                </div>
            </div>
        </nav>
    </header>
    <main class="container">
